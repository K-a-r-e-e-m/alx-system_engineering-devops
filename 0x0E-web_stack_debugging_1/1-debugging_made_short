#!/usr/bin/env bash
# Replace all ports 8080 with port 80 
# -i for edit in place, s for string and g for golbal (all match)
sed -i "s/8080/80/g" /etc/nginx/sites-enabled/default
service nginx restart
